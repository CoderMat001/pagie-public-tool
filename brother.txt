# Brother iPrint&Scan Desktop — confirmed by the user (8/27) to be a genuinely universal app,
# despite the link only being reachable through one specific model's support page (Brother's site
# hides the universal download inside per-model sections rather than a general download page).
#
# Link found 8/27: a "downloadhowto.aspx" page for the MFC-J6930DW's support section. This dev
# environment can't reach brother.com to verify what it actually returns (network egress proxy
# blocks it), so this is unverified from here — first real test is whatever happens when the app's
# Install button is actually clicked. ScanAppInstallService.InstallViaDownloadAsync has a guard for
# the case where a Download URL turns out to return an HTML page instead of a real file (fails
# clearly instead of saving/launching the wrong thing), so a bad link here should surface plainly
# rather than silently misbehaving.
#
# If this URL ever goes stale or turns out wrong, revert to:
#   Mechanism=Page
#   Url=https://support.brother.com
Mechanism=Download
Url=https://support.brother.com/g/b/downloadhowto.aspx?c=us&lang=en&prod=mfcj6930dw_us_eu_as&os=10068&dlid=dlf106359_000&flang=4&type3=10272
#
# NOTE (8/27): this file only holds install info now — whether Brother's app is already installed
# is checked in the app itself (ScanAppInstallService.InstallChecks), not from a field here. See
# that file's comment for why (a real bug found with Windows Scan: a wrong/guessed search term
# living in a plain-text file had no compiler or review catching it).
