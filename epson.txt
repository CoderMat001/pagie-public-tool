# Epson ScanSmart — confirmed by the user (8/27) to be a genuinely universal app (works across
# Epson models), despite the URL being filed under one specific model's page — same "vendor hides
# the universal download inside per-model sections" situation as Brother.
#
# Link found 8/27: ftp.epson.com's own direct file host, real .exe extension. This dev environment
# can't reach ftp.epson.com to verify it (network egress proxy blocks it), so this is unverified
# from here — first real test is whatever happens when the app's Install button is actually
# clicked. ScanAppInstallService.InstallViaDownloadAsync has a guard for a Download URL that turns
# out to return an HTML page instead of a real file, so a bad link should surface clearly rather
# than silently misbehaving.
#
# If this URL ever goes stale or turns out wrong, revert to:
#   Mechanism=Page
#   Url=https://epson.com/Support/Printers
Mechanism=Download
Url=https://ftp.epson.com/drivers/ScanSmart_MFP_3720.exe
# Best-effort — same bundled-with-driver reasoning as canon.txt.
AppSearchName=Epson Scan|ScanSmart
